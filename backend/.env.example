DATABASE_URL="postgresql://user:password@localhost:5432/sistema_reservas?schema=public"
PORT=4000

# Mailtrap (sandbox para pruebas — reemplazar con SMTP real en producción)
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=tu_usuario_aqui
SMTP_PASS=tu_password_aqui
SMTP_FROM=noreply@ucb-deportes.dev

CORS_ORIGIN="http://localhost:5173"

# Modo desarrollo: permite bypass de auth si no hay public.pem
ALLOW_DEV_MOCK=true
